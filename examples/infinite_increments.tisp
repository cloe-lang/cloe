(let (mapImpl map func list)
     (if (= list [])
         []
         (prepend (func (first list))
                  (map func (rest list)))))
(let map (y mapImpl))

(let (numbersImpl numbers i)
     (prepend i (numbers (+ i 1))))
(let numbers (y numbersImpl))

(seq ..(map write (numbers 0)))
